/* import { VStack, Text, ScrollView, View, Icon } from 'native-base';
import { SafeAreaView } from 'react-native-safe-area-context';

import { MaterialIcons } from '@expo/vector-icons';
import { RequestsRoutes } from '@routes/requests.routes';

export function Requests() {
    return (
        <SafeAreaView style={{ flex: 1 }}>
            <VStack alignItems="flex-start" p={6}>
                <View p={1} borderRadius="9999" borderWidth={1} borderColor="blue.400" my={2} >
                    <Icon as={MaterialIcons} name="attach-money" size={4} color="blue.400" />
                </View>
                <Text
                    fontFamily="heading"
                    fontSize="2xl"
                    color="blue.400"
                >
                    Meus Pedidos
                </Text>
            </VStack>
            
            <RequestsRoutes />

        </SafeAreaView>
    );
} */